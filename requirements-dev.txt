# Chỉ cần khi phát triển / chạy CI. Không cần để dùng hai MCP server.
#   pip install -r requirements-dev.txt

ruff>=0.16.5        # lint (cấu hình trong pyproject.toml)
pytest>=9.1.1         # test (tests/)
